% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/protegR2_startup_check.R
\name{protegR2_startup_check}
\alias{protegR2_startup_check}
\title{Vérifier la configuration au démarrage}
\usage{
protegR2_startup_check(config_global, pool = NULL)
}
\arguments{
\item{config_global}{Liste de configuration globale retournée par
`s3readRDS_HL()` ou `protegR2_local_config()`.}

\item{pool}{Pool de connexion PostgreSQL créé avec `pool::dbPool()`.
Ignoré si le backend n'est pas `"postgres"`. Défaut `NULL`.}
}
\value{
`invisible(NULL)`. Les messages sont émis via `message()`.
}
\description{
Détecte le backend configuré et vérifie que les ressources nécessaires sont
accessibles (fichier d'utilisateurs, tables, pool de connexion). Affiche un
message diagnostique dans la console avec les actions correctives si une
ressource manque.

À appeler dans `global.R`, après le chargement de `config_global` et du
`pool` (si postgres), mais avant `shinyApp()`.
}
