% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/protegR2_init.R
\name{protegR2_setup}
\alias{protegR2_setup}
\title{Initialiser le backend de données d'un projet protegR2}
\usage{
protegR2_setup(config_global, pool = NULL)
}
\arguments{
\item{config_global}{Liste de configuration du projet (chargée depuis S3).}

\item{pool}{Objet pool postgres. Requis si `user_config_backend == "postgres"`, ignoré sinon.}
}
\value{
Rien.
}
\description{
Orchestre l'initialisation complète selon le `user_config_backend` défini
dans `config_global$protegR2$user_config_backend` :

- `"none"` ou `"s3"` : crée uniquement `users_auth.rds` sur S3
- `"postgres"` : crée `users_auth.rds` sur S3 ET le schéma `protegr2` dans postgres

`users_auth.rds` est toujours créé sur S3 car l'authentification reste
sur S3 dans tous les modes.
}
\examples{
\dontrun{
  # Mode S3 uniquement
  protegR2_setup(config_global)

  # Mode postgres
  pool <- pool::dbPool(...)
  protegR2_setup(config_global, pool = pool)
  pool::poolClose(pool)
}
}
